const adminRoutes = require('./adminRoutes');
const ownerRoutes = require('./ownerRoutes');
const orderRoutes = require('./orderRoutes');

const routes = {adminRoutes, ownerRoutes, orderRoutes};

module.exports = routes;